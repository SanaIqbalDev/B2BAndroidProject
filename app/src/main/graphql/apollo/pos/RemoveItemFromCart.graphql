mutation removeItemFromCart($removeItem: RemoveItemFromCartInput!){
    removeItemFromCart(input:$removeItem){
        cart{
            items{
                uid
                product{
                    name
                }
            }
        }
    }
}